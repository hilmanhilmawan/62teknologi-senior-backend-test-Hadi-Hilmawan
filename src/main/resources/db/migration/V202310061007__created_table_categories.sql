CREATE TABLE `enam_dua_teknologi`.`categories` (
    `id` bigint(10) NOT NULL AUTO_INCREMENT,
    `reft_id` varchar(50) DEFAULT NULL,
    `alias` varchar(100) DEFAULT NULL,
    `title` varchar(100) DEFAULT NULL,
     PRIMARY KEY (`id`)
);